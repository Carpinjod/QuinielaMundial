services:
  quiniela:
    build: .
    container_name: quiniela-mundial
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
      - PORT=8080
    restart: unless-stopped
